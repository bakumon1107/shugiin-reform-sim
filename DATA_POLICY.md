# データの扱いに関する方針

このリポジトリの `data/` には、落選者を含む衆議院議員総選挙の候補者について、
氏名・年齢・党派・職業・得票数・惜敗率が入っています（回によっては性別も）。
その扱いについての方針です。

## なぜ収録しているか

- いずれも **総務省が「結果調」として既に公表している情報**です。
  出典・sha256 は [raw/SOURCE.md](raw/SOURCE.md) に記録しています。
- 公職の候補者としての公的活動に関する情報であり、
  個人情報保護法上の **要配慮個人情報は含みません**（信条・病歴・犯罪歴等はありません）。
- 目的は選挙制度改革案の比較シミュレーションという統計・研究用途です。

## 引いている線

PDFは人間が読むには不便な形式で、CSVにすると**名寄せ（他データとの結合）が容易になります**。
公表済みの情報でも、機械可読化そのものがリスクを上げるという認識のもと、次を守ります。

### 1. 原典に無い個人属性を足さない

`data/` に入れてよいのは、出典PDFに印字されている項目だけです。
生年月日・住所・連絡先・顔写真・SNSアカウント・学歴・家族構成などは、
たとえ他所で公開されていても**追加しません**。

### 2. 空欄を推定で埋めない

第49回（2021年）以降の表(13)には「性別」列がありません。
このため `smd_candidates.csv` の `gender` 列は、第47・48回では `男`/`女`、
第49回以降は**空文字**です。

**氏名や敬称からの推定で埋めることはしません。** 推定値と実測値が同じ列に混ざると、
利用者が区別できなくなるためです。分析で性別が必要な場合は、
`gender` が入っている回（`h26-12-14` / `h29-10-22`）に限って使ってください。

この方針は検証項目 **E5** で機械的に確認しています（下記）。

### 3. 他ソースと結合したデータをこのリポジトリに入れない

SNS・報道・名簿・政治資金収支報告書などとの突合結果は、
本リポジトリの成果物には含めません。シミュレータが必要とするのは
「選挙区・党派・得票数・名簿順位」であり、個人の属性ではありません。

### 4. 氏名は突合のためだけに使う

氏名は、表(13)と表(11)の重複立候補を対応づけるために保持しています
（[README](README.md) の検証 C10）。それ以外の目的では使いません。

### 5. 修正・削除の申し出には対応する

事実誤認や削除の求めがあれば、出典PDFと突き合わせたうえで対応します。
ただし出典PDFの記載そのものを書き換えることはできないため、
その場合は総務省の公表内容が優先されます。

## 対象外にしているもの

- **最高裁判所裁判官国民審査**（出典PDFの第2部）は取り込んでいません。
  シミュレータに不要であり、収録範囲を必要最小限にとどめるためです。

## 機械的に確認していること

`python -m verify.run_verify <election_id>` が毎回チェックします。

| ID | 内容 |
|---|---|
| E5 | `gender` 列が、出典PDFに「性別」列がある回だけ埋まっていること（部分的な推定混入の検出） |
| E6 | `smd_candidates` / `pr_list_entries` の列が宣言したスキーマと一致すること（原典に無い属性列の混入の検出） |

いずれかが崩れると検証が FAIL し、リグレッションが落ちます。
